function string(a, b, c) {
    let strlegth = (a.length + b.length + c.length);
    let average = Math.floor(strlegth / 3);
    console.log(strlegth)
    console.log(average)
}

string('chocolate', 'ice cream', 'cake')
string('pasta', '5', '22.3')
